{
  "name": "ssh-box-monorepo",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/common/*"
  ],
  "scripts": {
    "test": "npm test --workspaces",
    "build": "npm run build --workspaces"
  }
}